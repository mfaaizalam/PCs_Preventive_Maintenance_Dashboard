# URL of the FastAPI backend (backend/app/main.py).
# Locally this is usually http://localhost:8000 (uvicorn default) or
# whatever host/port your team runs the API on.
VITE_API_BASE_URL=http://localhost:8000
